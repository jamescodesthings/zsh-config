# 2026-09-20 18:36 BST Claude Code CLI, base stack, Xcode and asdf policy

## Session summary
After a discussion the user asked for, built: `configs/brew/Brewfile` (base stack, installed by `010-brew`), `brew-isolated` moved into `updaters/_stub`, `035-xcode`, a policy-driven `040-asdf` reading `configs/asdf/update-policy`, a slimmer `050-node`, and install stubs in `060-uv` and `070-bun`. A code review found that shortened and CRLF version pins were ignored; fixed in 0c59c90. `040-asdf` then ran for real and every toolchain was sanity-checked.

## Task status
- [x] Brewfile and homebrew bootstrap in `010-brew`
- [x] `035-xcode` (tested against fakes only)
- [x] `040-asdf` policy, pins, cleanup, reshim; real run verified
- [x] uv and bun stubs
- [x] Review fixes
- [ ] User: full `update` run with real sudo
- [ ] User: read the improvements doc and choose ranks

## Reference plans
- [cheatsheets/update.md](../cheatsheets/update.md)
- [configs/asdf/update-policy](../configs/asdf/update-policy)
- [configs/brew/Brewfile](../configs/brew/Brewfile)

## Handoff notes
- `asdf latest python` returns the free-threaded `3.14.7t`; versions come from `asdf list all` filtered to plain numbers.
- zsh associative array subscripts cannot hold a space in an assignment (`PINNED[a b]=x` is a bad pattern); keys are `tool@version` built into a variable first.
- The user's default python packages were reinstalled into 3.14.7 by asdf (26 packages), from the tracked `configs/python/.default-python-packages`.
- rustup-init prints an ignorable `error:` line when a rust shim is on PATH; `RUSTUP_INIT_SKIP_PATH_CHECK=yes` is now exported by `040-asdf`.
- Another session committed 86fde14 (kitty remote control) to this repo on 2026-09-20; it was pushed along with 339ae96.

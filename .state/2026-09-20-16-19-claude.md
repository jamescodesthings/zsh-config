# 2026-09-20 16:19 BST Claude Code CLI, update hardened after first real runs

## Session summary
The user ran `update` twice with real sudo and reported a second sudo prompt mid-run plus a noisy error log. Root causes: Homebrew runs `sudo --reset-timestamp` on every command; `900-agent-forge` treated any `[warn]` line as a failure; `mas` warns once per app Spotlight has not indexed; `bun upgrade` appended a hardcoded completions line to `.zshrc`. All four fixed in 3cb50b4.

## Task status
- [x] Diagnose the second sudo prompt
- [x] Review both error logs
- [x] Fix brew, agent-forge, mas and bun issues
- [ ] User: confirm sudo isolation in a real terminal and rerun `update`
- [ ] On hold: choices from the three research docs

## Reference plans
- [cheatsheets/update.md](../cheatsheets/update.md)

## Handoff notes
- The sudo isolation rests on sudo keeping one session per terminal. It cannot be tested from an agent shell. Everything around it was: brew runs fine under `script` with stdin closed, exit codes pass through, and the log comes out free of control characters.
- A brew cask that needs root now fails instead of prompting; the updater prints the manual command.
- The macOS restart warnings in the 11:10 log were by design. The user upgraded to macOS 27 by hand that morning.

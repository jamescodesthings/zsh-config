# 2026-09-19 09:48 Claude Code CLI, project initialized

## Session summary
Ran `project-init` on a repo that already had a `CLAUDE.md`, so this was PROJECT_INIT.md case B. None of the twelve portable sections were present, so all were added under a `# Global workflow` heading with nothing overwritten. Added Required tooling and Testing/Running sections, filled Post-implementation checks, and seeded project memory, `.state/` and `.claude/runners.txt`. Committed and pushed as 773e380.

Also diagnosed `updaters/brew:27: unmatched "`. Reproduced it exactly by overwriting the pre-f36323b version of the script with the current one while it was running against a slow fake `brew`. The script is fine; zsh reads scripts incrementally, so saving one mid-run breaks the parse.

## Task status
- [x] Sync portable sections into `CLAUDE.md`
- [x] Seed memory, state and runners
- [x] Diagnose the brew updater error
- [ ] Pick a zsh linting setup for VS Code
- [ ] Optional hardening of updaters against mid-run edits

## Reference plans
[PROJECT_INIT.md](file:///Users/jamesmacmillan/projects/personal/agent-forge/PROJECT_INIT.md)

## Handoff notes
- Sync baseline is agent-forge changelog entry 2026-09-07, recorded in project memory `project_context.md`.
- `zsh -i -c exit` without a TTY always prints `can't change option: monitor` and `gitstatus failed to initialize`; exit code 0 is the pass signal.
- `wrappers/cp` shadows `cp` with a progress wrapper that prints terminal control codes; use `/bin/cp` in scripted checks.
- `updaters/brew` and `updaters/zz-agent-forge` carry the user's uncommitted work and were deliberately left out of the commit.

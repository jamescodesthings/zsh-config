# remap prefix from 'C-b' to 'C-a'
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

bind-key -n C-M-q kill-server

# split panes using | and -
unbind \\
bind \\ split-window -h
bind - split-window -v
unbind '"'
unbind %

# switch panes using Alt-arrow without prefix
# bind -n M-Left select-pane -L
# bind -n M-Right select-pane -R
# bind -n M-Up select-pane -U
# bind -n M-Down select-pane -D

# Enable mouse control (clickable windows, panes, resizable panes)
set -g mouse on

# statusbar: https://medium.com/hackernoon/customizing-tmux-b3d2a5050207
# replacements: https://linux.die.net/man/1/tmux
# if prefix: #{?client_prefix,#[bg=colour2],}
# if zoomed: #{?window_zoomed_flag, ,ZOOM}
# set -g status-position bottom
# set -g status-justify left
set -g status-style 'fg=black bg=colour92'

# set -g status-left ''
# set -g status-left-length 10

# set -g status-right-style 'fg=colour63'
# set -g status-right '%Y-%m-%d %H:%M '
# set -g status-right-length 50

# set -g window-status-current-style 'fg=color39'
# set -g window-status-current-format '#[fg=colour42]#I:#[fg=colour39]#W#[fg=colour184]#F'

# set -g window-status-style 'fg=colour30'
# set -g window-status-format '#I:#[fg=colour36]#W#F'

# set -g window-status-bell-style 'fg=black bg=colour39'

# # messages
# set -g message-style 'fg=black bg=colour39'
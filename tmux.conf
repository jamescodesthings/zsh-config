# remap prefix from 'C-b' to 'C-a'
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

bind-key -n C-M-q kill-server

# split panes using | and -
unbind \\
bind \\ split-window -h
bind - split-window -v
unbind '"'
unbind %

# switch panes using Alt-arrow without prefix
# bind -n M-Left select-pane -L
# bind -n M-Right select-pane -R
# bind -n M-Up select-pane -U
# bind -n M-Down select-pane -D

# Enable mouse control (clickable windows, panes, resizable panes)
set -g mouse on

## Tmux2k settings
# set the left and right plugin sections
set -g @tmux2k-left-plugins "session"
set -g @tmux2k-right-plugins "cpu ram bandwidth battery"
# control refresh rate, applies to bandwidth, ping
set -g @tmux2k-refresh-rate 5

# use a theme
set -g @tmux2k-theme 'onedark'

# to show icons only
set -g @tmux2k-icons-only false

# Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'laktak/extrakto'
set -g @plugin 'aless3/tmux-click-copy'
set -g @plugin '2kabhishek/tmux2k'

# If xsel is available
if-shell 'command -v xsel >/dev/null 2>&1' {
  set -g @click-copy-command "xsel -ib"
  set -s copy-command 'xsel -ib'
  bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "xsel -ib"
  bind-key -T copy-mode MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "xsel -ib"
}

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

## Todo: colors:

# # Override Theme
# set -g @tmux2k-text 'black'
# set -g @tmux2k-bg-main 'black'
# set -g @tmux2k-bg-alt 'black'
# set -g @tmux2k-prefix-highlight '#ff5faf'

# set -g @tmux2k-message-fg '#ffffff'
# set -g @tmux2k-message-bg '#360074'

# set -g @tmux2k-pane-border '#444444'
# set -g @tmux2k-pane-active-border '#360074'

# # Override Theme Base Colors
# set -g @tmux2k-light-green '#ccffcc'
# set -g @tmux2k-green '#3dd50a'
# set -g @tmux2k-dark-green '#006400'

# set -g @tmux2k-white '#ffffff'
# set -g @tmux2k-dark-gray '#3f3f4f'
# set -g @tmux2k-black '#000000'

# set -g @tmux2k-light-blue '#11dddd'
# set -g @tmux2k-blue '#1688f0'
# set -g @tmux2k-dark-blue '#0000CD'

# set -g @tmux2k-light-green '#ccffcc'
# set -g @tmux2k-green '#3dd50a'
# set -g @tmux2k-dark-green '#d7875f'

# set -g @tmux2k-light-orange '#FFA07a'
# set -g @tmux2k-orange '#FFA500'
# set -g @tmux2k-dark-orange '#FF4500'

# set -g @tmux2k-light-pink '#FFB6C1'
# set -g @tmux2k-pink '#FF69B4'
# set -g @tmux2k-dark-pink '#FF1493'

# set -g @tmux2k-light-purple '#DDA0DD'
# set -g @tmux2k-purple '#bf58ff'
# set -g @tmux2k-dark-purple '#4b0082'

# set -g @tmux2k-light-red '#E68a8a'
# set -g @tmux2k-red '#ff1f1f'
# set -g @tmux2k-dark-red '#800000'

# set -g @tmux2k-light-yellow '#fffacd'
# set -g @tmux2k-yellow '#ffd21a'
# set -g @tmux2k-dark-yellow '#b8860b'
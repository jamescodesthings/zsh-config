# reload

Reload a zsh autoloaded function without restarting the shell.

## Usage

- Reload a specific function:

`reload <function-name>`

- Reload all functions:

`reload all`

## Examples

- After editing the cheat function:

`reload cheat`

- After editing multiple functions:

`reload all`
